<% provide(:title, "#{@user.name} / #{@title}") %>

<%= render 'shared/user_stats' %>

<% if @users.any? %>
  <ul class="follow_list">
    <%= render @users %>
    <%= paginate @users %>
  </ul>
<% end %>